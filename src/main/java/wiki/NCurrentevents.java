package wiki;

import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;

public class NCurrentevents {
    @FindBy(id = "n-currentevents")
    private WebElement  nCurrentevents;
}
